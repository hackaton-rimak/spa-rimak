import { createNamedStyles } from "../../shared/interfaces/create_named_styles";

export const chartWrapperStyles = createNamedStyles({
  wrapper: {
    flex: 1,
    width: "400px",
    height: "350px",
    borderRadius: "20px",
    padding: "10px",
  },
});
